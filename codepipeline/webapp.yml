version: 0.2

phases:
  install:
    commands:
      - echo "No installation required"
  pre_build:
    commands:
      - echo "No pre-build required"
  build:
    commands:
      - aws s3 sync frontend/ s3://my-s3-bucket --delete
artifacts:
  files:
    - '**/*'
